const viewer=document.getElementById("viewer");const viewerImage=document.getElementById("viewer-image");const closeButton=viewer.querySelector(".close");document.querySelectorAll(".frame-button").forEach(button=>button.addEventListener("click",()=>{viewerImage.src=button.dataset.full;viewer.showModal()}));closeButton.addEventListener("click",()=>viewer.close());viewer.addEventListener("click",event=>{if(event.target===viewer)viewer.close()});viewer.addEventListener("close",()=>viewerImage.removeAttribute("src"));
