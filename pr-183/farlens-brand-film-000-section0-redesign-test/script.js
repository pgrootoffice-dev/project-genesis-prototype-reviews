const viewer=document.querySelector('#viewer');const full=viewer.querySelector('img');document.querySelectorAll('.frame-button').forEach((button)=>button.addEventListener('click',()=>{full.src=button.dataset.full;viewer.showModal()}));viewer.querySelector('.close').addEventListener('click',()=>viewer.close());viewer.addEventListener('click',(event)=>{if(event.target===viewer)viewer.close()});
